# Simple Apache configuration for Rankor React SPA
# Essential routing support for single-page applications

# Enable URL rewriting
RewriteEngine On

# SPA Routing - Redirect all requests to index.html except for existing files
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^.*$ /index.html [L]

# Basic security - prevent access to .htaccess itself
<Files ".htaccess">
Order Allow,Deny
Deny from all
</Files>